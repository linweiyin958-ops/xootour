# 标准草案：广州旅游景点数据采集规范（基于多源UGC与XOOTOUR架构）

> 本版本基于 XOOTOUR 景点系统架构，引入 L1~L5 数据度量层，面向广州千年商都与岭南文化核心特色定制。

---

## 一、核心设计原则

```
1. 面向「外宾体验与广州国际商贸中心形象」：提取国际通用的游玩决策，突出岭南文化底蕴与现代都市活力。
2. 面向「多源趋势引领」：利用马蜂窝的游记攻略与打卡机位、携程的结构化门票数据、TripAdvisor的外宾真实评分，反向筛选广州最具烟火气和时代感的景点。
3. 面向「全流程闭环」：严密结合结构化表单（基础、画廊、亮点、路线、设施、套餐），确保前台100%完美还原。
4. 所有字段必须可结构化 + 可 AI 理解：方便通过大模型自动从多源数据中抽提信息。
```

**数据治理原则：**

```
5. 多源校验：以马蜂窝作为主要 UGC 及图片源，高德地图作为坐标与基础地址源，携程/官方作为门票与须知核验源，TripAdvisor作为外宾视角补充源。
6. 数据血缘：每条景点记录需标注对应的数据来源链接或官方源，可追溯采集时间。
7. 合规优先：图片需进行去水印、去版权争议处理，内容通过 AI 进行二次转写拒绝全文抄袭。
```

---

## 二、数据分层结构（结合 XOOTOUR 系统板块）

根据 XOOTOUR 规范与数据分层逻辑，景点数据被拆解为以下五层：

```
L1：基础与概况层（必须）     —— 主表 tour_spot + 概况表 tour_spot_info（标识、广州行政区、地理坐标）
L2：游玩与须知层（核心）     —— 开放时间、交通方式、参观须知、门票套餐（tour_spot_package）
L3：出片与打卡层（差异化）   —— 马蜂窝游记热度快照、推荐打卡季节、核心受众标签
L4：AI 与深度内容层（体验）  —— 亮点体验（tour_spot_feature）+ 游玩路线（tour_spot_route）+ 服务设施（tour_spot_facility）
L5：质量与血源层（可信度）   —— 多源数据追踪、置信度评估、更新周期控制
```

### 核心表/子表联动设计

| 后台菜单版块 | 系统对应数据表名 | 对应采集/关联说明 |
|-------------|----------------|------------------|
| 景点列表 | `tour_spot` (主表) | 核心基础数据：名称、副标题(英文)、SEO描述、基础价格、主图、所属城市ID（广州） |
| 景点列表 (已融合) | `tour_spot_info` (概况表) | 详情介绍、详细地址、开放时间、交通方式、参观须知 |
| 景点画廊 | `tour_spot_banner` (子表) | 顶部的多张轮播图，利用马蜂窝/官方高清图，每个景点最少5张 |
| 亮点体验 | `tour_spot_feature` (子表) | 景点特色、亮点体验描述，由 AI 从马蜂窝游记热点词中提炼 |
| 游玩路线 | `tour_spot_route` (子表) | 推荐游览路线及关键打卡点信息，整合马蜂窝"保姆级路线" |
| 设施服务 | `tour_spot_facility` (子表) | 停车场、游客中心、WIFI、无障碍设施等，标注加粗项 |
| 景点套餐 | `tour_spot_package` (子表) | 门票价格及打包游玩套餐信息（部分免费景点可为空） |
| 分类配置 | `tour_spot_cate` | 定义广州景点的分类标签（如：现代地标、岭南文化、历史古迹、主题乐园、自然风光等） |

---

## 三、标准字段定义

---

### L1：基础与概况层（Base & Info）

| 字段名 | 类型 | 必须 | 说明 / 示例 |
|--------|------|------|------------|
| `spot_id` | INT | ✅ | 主键 ID（后台关联纽带） |
| `name_zh` | VARCHAR(200) | ✅ | 中文全名（如：广州塔、陈家祠、白云山） |
| `name_en` | VARCHAR(200) | ✅ | 英文全名（如：Canton Tower、Chen Clan Ancestral Hall） |
| `city_id` | INT | ✅ | 固定关联广州的城市ID |
| `district` | VARCHAR(100) | ✅ | 广州行政区（天河区、越秀区、荔湾区、海珠区、白云区、番禺区等） |
| `address_zh` | TEXT | ✅ | 中文详细地址（自高德核验） |
| `address_en` | TEXT | ✅ | 英文详细地址（面向外宾） |
| `lat` | DECIMAL(10,7) | ✅ | 地理坐标纬度（WGS84标准） |
| `lng` | DECIMAL(10,7) | ✅ | 地理坐标经度（WGS84标准） |
| `cover_img` | VARCHAR(500) | ✅ | 景点主图 / 封面图 URL |
| `seo_desc` | VARCHAR(500) | ✅ | SEO 描述：高度概括且包含关键词的景点简介 |
| `summary_zh` | TEXT | ✅ | 详情介绍（中）：AI 整合多源 UGC 的景点概况简介 |
| `summary_en` | TEXT | ✅ | 详情介绍（英）：面向外宾的景点概况简介 |

---

### L2：游玩与须知层（Operations & Packages）

| 字段名 | 类型 | 必须 | 说明 / 示例 |
|--------|------|------|------------|
| `open_hours` | TEXT | ✅ | 开放时间（如广州塔"09:30-22:30"，长隆野生动物世界"09:30-18:00"） |
| `transportation` | TEXT | ✅ | 交通方式：地铁/公交指南（如"地铁3号线/APM线广州塔站"） |
| `visit_notice` | TEXT | ✅ | 参观须知：购票方式、携带物品限制、是否需要提前预约、广交会期间人流提示等 |
| `base_price` | DECIMAL(10,2) | ✅ | 起步参考价格（0.00 代表免费开放，如花城广场） |
| `rating` | DECIMAL(2,1) | ✅ | 核心综合评分（结合马蜂窝推荐度、携程评分与TripAdvisor外宾评分） |

---

### L3：出片与打卡层（Multi-Source UGC Optimization）

| 字段名 | 类型 | 必须 | 说明 / 示例 |
|--------|------|------|------------|
| `ugc_notes_count` | INT | ❌ | 马蜂窝相关游记数量 / 热度指标 |
| `best_visit_season` | VARCHAR(100) | ❌ | 最佳游玩季节/时间（如："春季/木棉花开"、"珠江夜游19:00-22:00"） |
| `photo_spots` | JSON | ❌ | 最佳打卡机位（来自马蜂窝游记）：`[{"position":"花城广场中轴线","tip":"拍摄广州塔全景最佳位置"}]` |
| `crowd_tags` | JSON | ❌ | 适合人群标签：`["Solo", "Couple", "Family-friendly", "Foodie", "Photography"]` |

---

### L4：AI 与深度内容层（Sub-Tables for Experience）

📸 **子表 1：`tour_spot_banner`**（景点画廊多图）

| 字段 | 类型 | 说明 |
|------|------|------|
| `spot_id` | INT | 关联主景点 ID |
| `image_url` | VARCHAR(500) | 图片URL（每景点最少5张） |
| `sort_order` | INT | 排序 |
| `photo_credit` | VARCHAR(100) | 图片来源标注 |

💡 **子表 2：`tour_spot_feature`**（亮点体验）

| 字段 | 类型 | 说明 |
|------|------|------|
| `spot_id` | INT | 关联主景点 ID |
| `title` | VARCHAR(150) | 亮点标题（如："云端俯瞰羊城"、"百年岭南建筑精粹"） |
| `description` | TEXT | 深度打卡体验描述 |

🗺 **子表 3：`tour_spot_route`**（游玩线路）

| 字段 | 类型 | 说明 |
|------|------|------|
| `spot_id` | INT | 关联主景点 ID |
| `route_name` | VARCHAR(200) | 路线名称（如："珠江新城中轴线一日线"、"荔湾西关老城漫步线"） |
| `duration_hours` | DECIMAL(3,1) | 预计游玩时长 |
| `route_nodes` | JSON | 关键打卡节点列表 |
| `description` | TEXT | 详尽路线行进与避坑指南 |

🛡 **子表 4：`tour_spot_facility`**（服务设施）

| 字段 | 类型 | 说明 |
|------|------|------|
| `spot_id` | INT | 关联主景点 ID |
| `facility_name` | VARCHAR(100) | 设施名称（游客中心、行李寄存、无障碍通道、WIFI、母婴室等） |
| `is_bold` | BOOLEAN | 是否在前端加粗突出 |
| `description` | VARCHAR(255) | 补充说明（如："支持Visa/Master外卡购票"） |

---

### L5：质量与血源层（Quality & Lineage）

| 字段名 | 类型 | 必须 | 说明 / 示例 |
|--------|------|------|------------|
| `source_platform` | VARCHAR(50) | ✅ | 主数据源平台标识（Mafengwo, Amap, Ctrip, TripAdvisor） |
| `source_note_ids` | JSON | ❌ | 数据提炼所引用的核心数据源链接/ID列表 |
| `data_collected_at` | TIMESTAMP | ✅ | 数据抓取时间 |
| `data_quality_score` | DECIMAL(3,2) | ❌ | 数据完整度评分 0-1 |
| `is_ai_generated` | BOOLEAN | ✅ | 内容是否经由 AI 结构化清洗转写 |

---

## 四、广州景点筛选标准

📥 **收录标准（必须满足 ≥2 条）**

1. 马蜂窝包含"广州 [景点名]"关键词的高赞游记（点赞 > 100）数量不少于 8 篇。
2. 属于广州核心城市名片（广州塔、珠江夜游、陈家祠、长隆、白云山等）或岭南文化地标（永庆坊、沙面岛、上下九等）。
3. 属于国家级旅游景区（4A及以上）、国家级文保单位或具有鲜明广府文化特色的历史街区。
4. TripAdvisor 上有对应的英文景点页面及外宾评分。

❌ **排除标准**

- 在马蜂窝/TripAdvisor上近期大量被标记为"照骗/踩雷/已关闭/正在大修"的场所。
- 纯批发市场或非观光性质的工业区域。
- 已关闭拆除或长期维修无明确恢复开放时间的景点。

---

## 五、数据源优先级

在广州区域，数据采集与校验的优先级配置如下：

| 优先级 | 数据平台 | 核心获取内容 |
|--------|---------|------------|
| **P0** | 马蜂窝 (Mafengwo) | 主要 UGC 源：深度游记、景点攻略、推荐路线、打卡机位、真实体验避坑、高清图片。 |
| **P0** | 高德地图 API | 基建核心源：标准中文名称、高精度 WGS84 经纬度坐标、标准行政区划与标准地址。 |
| **P1** | 携程 (Ctrip) | 业务核验源：官方标准门票套餐、结构化开放时间、参观须知、用户点评。 |
| **P2** | TripAdvisor | 外宾补充源：英文景点介绍、外国游客真实点评与评分、外宾关注点。 |

---

## 六、AI 与脚本处理流程

### Step 1: 自动化采集

- **马蜂窝**：针对目标广州景点，抓取景点详情页及 Top 20 热门游记，获取图片、攻略路线、打卡机位及真实避坑指南。
- **携程**：抓取景点门票页结构化数据（开放时间、门票套餐、参观须知、用户评分）。
- **TripAdvisor**：抓取英文景点页面，获取外宾评分、英文介绍及真实英文点评。
- **高德地图**：通过 API 获取标准坐标、地址、行政区等结构化数据。

### Step 2: AI 结构化提取

大模型接收多源数据流，直接解构为系统所需的六大板块数据（详情介绍、画廊图片、亮点体验、游玩路线、设施服务、门票套餐）。

### Step 3: 后台导入与绑定

1. 脚本通过 API 先行在 `tour_spot` 和 `tour_spot_info` 中创建景点主数据，绑定广州城市 ID。
2. 脚本自动循环调用附属表 API，在各子表中填入主键 ID，完成全自动的数据强关联绑定。

---

## 七、版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| V1 | 2026-06-12 | 初始草案，基于上海模板定制广州版 |
